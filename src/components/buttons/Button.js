import React from 'react'
import styles from './button.module.css'

const Button = () => {
    return (
        <div>
             <button className={styles.getbtn}>GET IT NOW</button>
        </div>
    )
}

export default Button
